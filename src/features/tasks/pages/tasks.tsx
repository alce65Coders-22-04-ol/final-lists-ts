import tasks from './tasks.module.css';

function TasksPage() {
    return (
        <section className={tasks.host}>
            <h2>Página Tasks</h2>
            <p>Lista de tareas</p>
        </section>
    );
}

export default TasksPage;
